from gtts import gTTS
import os
from datetime import datetime

def jarvis_wish():
    # १. सध्याची वेळ मिळवणे
    hour = int(datetime.now().strftime("%H"))
    current_time = datetime.now().strftime("%I:%M %p")
    
    # २. वेळेनुसार मेसेज ठरवणे
    if hour >= 4 and hour < 12:
        greeting = "शुभ सकाळ अजिंक्य सर!"
    elif hour >= 12 and hour < 17:
        greeting = "शुभ दुपार अजिंक्य सर!"
    elif hour >= 17 and hour < 21:
        greeting = "शुभ संध्याकाळ अजिंक्य सर!"
    else:
        greeting = "शुभ रात्री अजिंक्य सर! आता विश्रांती घेण्याची वेळ झाली आहे."

    full_message = f"{greeting} सध्या {current_time} झाले आहेत."
    
    print(f"🤖 जार्विस: {full_message}")

    # ३. आवाज तयार करणे (मराठीत)
    tts = gTTS(text=full_message, lang='mr')
    tts.save("jarvis_wish.mp3")
    
    print("✅ जार्विसने तुमच्यासाठी विशिंग फाईल तयार केली आहे (jarvis_wish.mp3)")

# रन करा
jarvis_wish()
